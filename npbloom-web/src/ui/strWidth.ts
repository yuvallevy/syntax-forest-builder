import mockStrWidth from '../__tests__/__mocks__/mockStrWidth';

// Overwrite this with an actual string width measuring function when the time comes
const strWidth = mockStrWidth;

export default strWidth;
